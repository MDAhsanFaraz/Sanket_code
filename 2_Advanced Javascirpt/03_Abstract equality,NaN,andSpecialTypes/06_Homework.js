function checkDirection(value)
{
          // val is always a number 
          // tell us wheter val is -ve value or a +ve value 
          // if val is -ve return  'left' otherwise if val is +ve return 'right'  
}